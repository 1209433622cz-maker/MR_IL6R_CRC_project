# Normalize text files
*.md text eol=lf
*.R text eol=lf
*.ps1 text eol=crlf
*.csv text
*.tsv text
*.bib text
*.ris text

# Optional Git LFS example:
# If you decide to version large PDFs or ZIP files, install Git LFS first and uncomment:
# *.pdf filter=lfs diff=lfs merge=lfs -text
# *.zip filter=lfs diff=lfs merge=lfs -text
